# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Superpowers brainstorm sessions
.superpowers/

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.claude
.codebase_patterns
.CODEBASE_PATTERNS
.CODEBASE_PATTERNS.md
AUDIT-REPORT.md
CLAUDE.md
design-reference.html

# Internal HIPAA compliance checklist — keep out of git (contains
# infrastructure notes / audit gaps that shouldn't sit in version history)
docs/HIPAA_COMPLIANCE_CHECKLIST.md

# Dev-only Supabase seed data — never commit (contains test credentials)
supabase/seeds/

# Supabase CLI local link state — machine-local, changes every `supabase link`
supabase/.temp/
tmp-calibrate/
