# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Superpowers brainstorm sessions
.superpowers/

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.claude
.codebase_patterns
.CODEBASE_PATTERNS
.CODEBASE_PATTERNS.md
AUDIT-REPORT.md
CLAUDE.md
design-reference.html

# Internal HIPAA compliance docs — keep out of git (the checklist is a
# generic plan; the audit lists specific exploitable findings that
# absolutely should not sit in version history before they're fixed)
docs/HIPAA_COMPLIANCE_CHECKLIST.md
docs/HIPAA_CODE_AUDIT.md

# Dev-only Supabase seed data — never commit (contains test credentials)
supabase/seeds/

# Supabase CLI local link state — machine-local, changes every `supabase link`
supabase/.temp/
tmp-calibrate/
